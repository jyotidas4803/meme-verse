<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Meme World</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        ::-webkit-scrollbar {
            display: none;
        }
        html {
            scrollbar-width: none;
        }
    </style>
</head>
<body class="flex items-center justify-center h-screen bg-gray-900 text-white">

    <div class="w-full max-w-sm bg-gray-800 p-6 rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold text-center">Login</h1>

        <% if (error) { %>
            <p class="text-red-500 text-center mt-2"><%= error %></p>
        <% } %>

        <form action="/login" method="POST" class="mt-6">
            <label class="block mb-2">Username</label>
            <input type="text" name="username" class="w-full px-4 py-2 text-black rounded focus:outline-none" required>

            <label class="block mt-4 mb-2">Password</label>
            <input type="password" name="password" class="w-full px-4 py-2 text-black rounded focus:outline-none" required>

            <button type="submit" class="w-full mt-4 bg-yellow-500 hover:bg-yellow-600 text-black font-bold py-2 rounded">
                Login
            </button>
        </form>
    </div>

</body>
</html>
